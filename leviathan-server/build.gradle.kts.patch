--- a/paper-server/build.gradle.kts
+++ b/paper-server/build.gradle.kts
@@ -12,16 +_,30 @@
 }
 
 val paperMavenPublicUrl = "https://repo.papermc.io/repository/maven-public/"
+val leviathanMavenPublicUrl = "https://maven.leafmc.one/snapshots/" // Leviathan - project setup - Add publish repo
 
 dependencies {
     mache("io.papermc:mache:26.2+build.1")
-    paperclip("io.papermc:paperclip:3.0.4")
+    paperclip("cn.dreeam:quantumleaper:1.0.0-SNAPSHOT") // Leviathan - project setup - Use own paperclip fork
 }
 
 paperweight {
     minecraftVersion = providers.gradleProperty("mcVersion")
     gitFilePatches = false
 
+    // Leviathan start - project setup
+    val leviathan = forks.register("leviathan") {
+        upstream.patchDir("paperServer") {
+            upstreamPath = "paper-server"
+            excludes = setOf("src/minecraft", "patches", "build.gradle.kts")
+            patchesDir = rootDirectory.dir("leviathan-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+    }
+
+    activeFork = leviathan
+    // Leviathan end - project setup
+
     updatingMinecraft {
         // oldPaperCommit = "d4fe85375af18bfa88f44d7c1e6a61904ae550cc"
     }
@@ -31,6 +_,7 @@
     libraryRepositories.addAll(
         "https://repo.maven.apache.org/maven2/",
         paperMavenPublicUrl,
+        leviathanMavenPublicUrl // Leviathan - project setup - Add publish repo
     )
 }
 
@@ -86,7 +_,22 @@
     }
 }
 
-val log4jPlugins = sourceSets.create("log4jPlugins")
+// Leviathan start - project setup
+sourceSets {
+    main {
+        java { srcDir("../paper-server/src/main/java") }
+        resources { srcDir("../paper-server/src/main/resources") }
+    }
+    test {
+        java { srcDir("../paper-server/src/test/java") }
+        resources { srcDir("../paper-server/src/test/resources") }
+    }
+}
+
+val log4jPlugins = sourceSets.create("log4jPlugins") {
+    java { srcDir("../paper-server/src/log4jPlugins/java") }
+}
+// Leviathan end - project setup
 configurations.named(log4jPlugins.compileClasspathConfigurationName) {
     extendsFrom(configurations.compileClasspath.get())
 }
@@ -109,8 +_,21 @@
 }
 
 dependencies {
-    implementation(project(":paper-api"))
+    implementation(project(":leviathan-api")) // Leviathan - project setup
+
+    // Leviathan start - Libraries
+    implementation("com.github.thatsmusic99:ConfigurationMaster-API:v2.0.0-rc.3") { // Leviathan config
+        exclude(group = "org.yaml", module = "snakeyaml")
+    }
+    implementation("com.github.luben:zstd-jni:1.5.7-9") // Luminol - Buffered Linear
+    implementation("net.openhft:zero-allocation-hashing:2026.0") // Luminol - Buffered Linear
+    implementation("com.github.ben-manes.caffeine:caffeine:3.2.4")
+    // Leviathan end - Libraries
+
     implementation("ca.spottedleaf:leafpile:1.0.0")
+    // Leviathan - Bump Dependencies
+    // TODO: To check whether the error on shutdown on Windows gets fixed.
+    // TODO: Introduced in jline3#1576, tested on jline 4.1.3
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
     implementation("net.minecrell:terminalconsoleappender:1.3.0")
@@ -127,14 +_,20 @@
     runtimeOnly(log4jPlugins.output)
     alsoShade(log4jPlugins.output)
 
-    implementation("com.velocitypowered:velocity-native:3.4.0-SNAPSHOT") {
+    implementation("com.velocitypowered:velocity-native:3.5.0-SNAPSHOT") { // Leviathan - Bump Dependencies
         isTransitive = false
     }
-    implementation("io.netty:netty-codec-haproxy:4.2.15.Final") // Add support for proxy protocol
+    implementation("io.netty:netty-codec-haproxy:4.2.17.Final") // Add support for proxy protocol
     implementation("org.apache.logging.log4j:log4j-iostreams:2.26.0")
-    implementation("org.ow2.asm:asm-commons:9.9.1")
+    implementation("org.ow2.asm:asm-commons:9.10.1") // Leviathan - Bump Dependencies
     implementation("org.spongepowered:configurate-yaml:4.2.0")
 
+    // Purpur start
+    implementation("org.mozilla:rhino-runtime:1.7.15.1")
+    implementation("org.mozilla:rhino-engine:1.7.15.1")
+    implementation("dev.omega24:upnp4j:1.0")
+    // Purpur end
+
     // Deps that were previously in the API but have now been moved here for backwards compat, eventually to be removed
     runtimeOnly("commons-lang:commons-lang:2.6")
     runtimeOnly("org.xerial:sqlite-jdbc:3.49.1.0")
@@ -145,40 +_,57 @@
     }
 
     testImplementation("io.github.classgraph:classgraph:4.8.184") // For mob goal test
-    testRuntimeOnly("org.junit.platform:junit-platform-launcher:6.0.3")
-    testImplementation("org.junit.jupiter:junit-jupiter:6.0.3")
-    testImplementation("org.junit.platform:junit-platform-suite-engine:6.0.3")
-    testImplementation("org.hamcrest:hamcrest:2.2")
-    testImplementation("org.mockito:mockito-core:5.22.0")
-    mockitoAgent("org.mockito:mockito-core:5.22.0") { isTransitive = false } // Configure mockito agent that is needed in newer java versions
-    testImplementation("org.ow2.asm:asm-tree:9.9.1")
+    // Leviathan start - Bump Dependencies
+    testRuntimeOnly("org.junit.platform:junit-platform-launcher:6.1.0")
+    testImplementation("org.junit.jupiter:junit-jupiter:6.1.0")
+    testImplementation("org.junit.platform:junit-platform-suite-engine:6.1.0")
+    testImplementation("org.hamcrest:hamcrest:3.0")
+    testImplementation("org.mockito:mockito-core:5.23.0")
+    mockitoAgent("org.mockito:mockito-core:5.23.0") { isTransitive = false } // Configure mockito agent that is needed in newer java versions
+    testImplementation("org.ow2.asm:asm-tree:9.10.1")
+    // Leviathan end - Bump Dependencies
     testImplementation("org.junit-pioneer:junit-pioneer:2.3.0") // CartesianTest
 
+    // Leviathan start - Bump Dependencies
     // Spark
-    implementation("me.lucko:spark-api:0.1-20240720.200737-2")
-    implementation("me.lucko:spark-paper:1.10.172")
-}
+    implementation("me.lucko:spark-api:0.1-dev-f181ccf-SNAPSHOT")
+    implementation("me.lucko:spark-paper:1.10-dev-f181ccf-SNAPSHOT")
+
+    implementation("io.netty:netty-all:4.2.17.Final")
+    // Leviathan end - Bump Dependencies
+}
+
+// Gale start - hide irrelevant compilation warnings
+tasks.withType<JavaCompile> {
+    val compilerArgs = options.compilerArgs
+    compilerArgs.add("-Xlint:-module")
+    compilerArgs.add("-Xlint:-removal")
+    compilerArgs.add("-Xlint:-dep-ann")
+    compilerArgs.add("--add-modules=jdk.incubator.vector") // Gale - Pufferfish - SIMD support
+    compilerArgs.addAll(listOf("-Xmaxerrs", "513317")) // Leviathan - Increase compiling error count limit
+}
+// Gale end - hide irrelevant compilation warnings
 
 tasks.jar {
     manifest {
         val git = Git(rootProject.layout.projectDirectory.path)
         val mcVersion = rootProject.providers.gradleProperty("mcVersion").get()
         val build = System.getenv("BUILD_NUMBER") ?: null
-        val buildTime = providers.environmentVariable("BUILD_STARTED_AT").map(Instant::parse).orElse(Instant.EPOCH).get()
+        val buildTime = Instant.now() // Leviathan - project setup - Always use current as build time
         val gitHash = git.exec(providers, "rev-parse", "--short=7", "HEAD").get().trim()
         val implementationVersion = "$mcVersion-${build ?: "DEV"}-$gitHash"
         val date = git.exec(providers, "show", "-s", "--format=%ci", gitHash).get().trim()
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Paper",
+            "Implementation-Title" to "Leviathan", // Leviathan - Rebrand
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Paper",
+            "Specification-Title" to "Leviathan", // Leviathan - Rebrand
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Paper Team",
-            "Brand-Id" to "papermc:paper",
-            "Brand-Name" to "Paper",
+            "Specification-Vendor" to "Vospek", // Leviathan - Rebrand
+            "Brand-Id" to "vospek:leviathan", // Leviathan - Rebrand
+            "Brand-Name" to "Leviathan", // Leviathan - Rebrand
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
@@ -246,7 +_,7 @@
     jvmArgumentProviders.add(provider)
 }
 
-val generatedDir: java.nio.file.Path = layout.projectDirectory.dir("src/generated/java").asFile.toPath()
+val generatedDir: java.nio.file.Path = rootProject.layout.projectDirectory.dir("paper-server/src/generated/java").asFile.toPath() // Leviathan - project setup
 idea {
     module {
         generatedSourceDirs.add(generatedDir.toFile())
@@ -296,6 +_,8 @@
     val memoryGb = providers.gradleProperty("paper.runMemoryGb").getOrElse("2")
     minHeapSize = "${memoryGb}G"
     maxHeapSize = "${memoryGb}G"
+    jvmArgs("--enable-preview") // Gale - enable preview features for development runs
+    jvmArgs("--add-modules=jdk.incubator.vector") // Gale - Pufferfish - SIMD support
 
     doFirst {
         workingDir.mkdirs()
@@ -342,3 +_,78 @@
         }
     }
 }
+
+// Gale start - package license into jar
+tasks.register<Copy>("copyLicense") {
+    from(layout.projectDirectory.file("../paper-server/LICENSE.txt"))
+    into(layout.buildDirectory.dir("tmp/copiedlicense"))
+}
+
+tasks.processResources {
+    dependsOn("copyLicense")
+}
+
+sourceSets {
+    main {
+        resources {
+            srcDir(layout.buildDirectory.dir("tmp/copiedlicense"))
+        }
+    }
+}
+// Gale end - package license into jar
+
+// Gale start - branding changes - package license into jar
+// Based on io.papermc.paperweight.core.taskcontainers.PaperclipTasks
+
+tasks.named("createPaperclipJar") {
+    val name = rootProject.name
+    val version = project.version
+    val licenseFileName = "LICENSE.txt"
+    val licenseFilePath = layout.projectDirectory.dir("../paper-server/$licenseFileName").asFile
+
+    // Based on io.papermc.paperweight.core.taskcontainers.PaperclipTasks
+    val jarName = listOfNotNull(
+        name,
+        "paperclip",
+        version
+    ).joinToString("-") + ".jar"
+
+    // Based on io.papermc.paperweight.core.taskcontainers.PaperclipTasks
+    val zipFile = layout.buildDirectory.file("libs/$jarName").get().path
+
+    val rootDir = findOutputDir(zipFile)
+
+    doLast {
+
+        try {
+            unzip(zipFile, rootDir)
+
+            licenseFilePath.copyTo(rootDir.resolve(licenseFileName).toFile())
+
+            ensureDeleted(zipFile)
+
+            zip(rootDir, zipFile)
+        } finally {
+            @OptIn(kotlin.io.path.ExperimentalPathApi::class)
+            rootDir.toFile().deleteRecursively()
+        }
+
+    }
+}
+// Gale end - branding changes - package license into jar
+
+// Leviathan start - Leviathan JUnit test suite
+tasks.register<Test>("runLeviathanTests") {
+    group = LifecycleBasePlugin.VERIFICATION_GROUP
+    include("**/LeviathanTestSuite.class")
+    workingDir = temporaryDir
+    useJUnitPlatform {
+        forkEvery = 1
+    }
+
+    // Configure mockito agent that is needed in newer java versions
+    val provider = objects.newInstance<MockitoAgentProvider>()
+    provider.fileCollection.from(mockitoAgent)
+    jvmArgumentProviders.add(provider)
+}
+// Leviathan end - Leviathan JUnit test suite
